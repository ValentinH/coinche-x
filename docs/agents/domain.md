# Domain docs

This repository has one domain context.

Before exploring, read:

- `CONTEXT.md` at the repository root, when present
- relevant ADRs under `docs/adr/`, when present

Missing files are normal. Create `CONTEXT.md` lazily through domain modeling when terminology is first resolved. Create an ADR only for a hard-to-reverse, surprising trade-off.

Use glossary terms consistently in issues, plans, code, and tests. Surface conflicts with an existing ADR instead of silently overriding it.
