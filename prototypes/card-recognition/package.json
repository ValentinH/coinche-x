{
  "name": "coinche-card-recognition-prototype",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.recognition.json",
    "test": "node --test --experimental-strip-types tests/*.test.ts",
    "check": "pnpm test && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "typescript": "^5.9.3",
    "vite": "^8.1.5"
  }
}
